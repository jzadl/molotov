class Counter:
    def __init__(self, start):
        self.val = start
    def value(self):
        return self.val
    def increment(self):
        self.val = self.val + 1
c = Counter(5)
print(c.value())
c.increment()
print(c.value())
